import ComponentClientNotFound from 'commons/ClientComponentNotFound';
import { FC, memo } from 'commons/utils/react';

const ModalLogo: FC = () => <ComponentClientNotFound />;

export default memo(ModalLogo);
