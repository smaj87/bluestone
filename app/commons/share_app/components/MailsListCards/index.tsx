import { FC, memo } from 'commons/utils/react';

interface Props {
  index: number;
}

// todo on return coupons, change index to mid!
const MailsListCards: FC<Props> = () => null;

export default memo(MailsListCards);
