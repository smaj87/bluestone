import styled from 'commons/Goober';

export const FileNameStyled = styled('span')`
  min-width: 0;
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
`;

export const FileExtensionStyled = styled('span')`
  flex: auto;
`;
