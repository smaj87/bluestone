import styled from 'commons/Goober';

export const PrivacyPolicyStyled = styled('div')`
  position: relative;
  padding: 1.6rem;
  font-size: 1.4rem;
  line-height: 1.5;
  overflow: hidden;

  p {
    margin-bottom: 0.8rem;
    font-size: inherit;
    line-height: inherit;
  }
`;
