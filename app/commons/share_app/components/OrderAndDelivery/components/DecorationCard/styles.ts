import styled from 'commons/Goober';
import Icon from 'commons/Icon';

export const DecorationCardStyled = styled('div')`
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 1.6rem;
  width: 100%;
  background: var(--schema-bg--secondary);
`;

export const DecorationIcon = styled(Icon)`
  font-size: 4.8rem;
  color: var(--schema-txt--option);
`;
