export const KEY = 'Products';

export const FETCH_PRODUCTS = `${KEY}/FETCH_PRODUCTS` as const;
export const SET_GROUP_VISIBILITY = `${KEY}/SET_GROUP_VISIBILITY` as const;

export const ITEM_HEIGHT = 50;
export const GROUP_COUNT = 60;
